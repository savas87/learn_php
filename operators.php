<?php
    /**
     *  = equal
     * += a = a+x; a+=x
     * -= 
     * /=
     * *=
     * %=
     * . -> bind
     * .= ->  bind and elevated
     * 
     * compares
     *  == ist equal to
     *  === exactly
     */

     $a = 5;
     $a += 10;
     echo $a;
?>